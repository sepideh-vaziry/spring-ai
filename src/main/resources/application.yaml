spring:
  ai:
    openai:
      api-key: ${DEEP_SEEK_API_KEY}
      base-url: https://api.deepseek.com
      embedding:
        enabled: false
      chat:
        options:
          model: deepseek-chat
          temperature: 0.7
